// Существует только чтобы Chrome/Android считали сайт "устанавливаемым" (PWA).
// Никакого офлайн-кеша: данные о заказах должны быть всегда актуальными, каждый
// запрос просто проходит напрямую в сеть.
self.addEventListener('fetch', () => {});
